{
  "name": "ogxglobalhorizon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json && mkdir -p dist && cp index.html style.css main.js manifest.json sw.js dist/",
    "clean": "rm -rf dist",
    "deploy": "npm run build && wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260506.1",
    "typescript": "5.6.3",
    "wrangler": "4.86.0"
  }
}
